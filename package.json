{
  "name": "orora",
  "version": "0.0.1",
  "module": "./dist/index.js",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "private": false,
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "license": "MIT",
  "scripts": {
    "dev": "bun test --watch",
    "test": "bun test",
    "build": "bun run build.ts",
    "ci": "bun test && bun run build",
    "release": "bun test && bun run build && changeset publish"
  },
  "type": "module"
}
